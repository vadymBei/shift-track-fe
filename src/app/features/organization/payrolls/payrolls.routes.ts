import {Routes} from "@angular/router";
import {PayrollsPageComponent} from "./pages/payrolls-page/payrolls-page.component";

const routes: Routes = [
  { path: '', component: PayrollsPageComponent }
]

export default routes;
