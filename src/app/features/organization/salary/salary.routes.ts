import {Routes} from "@angular/router";
import {SalaryPageComponent} from "./pages/salary-page/salary-page.component";

const routes: Routes = [
  { path: '', component: SalaryPageComponent }
]

export default routes;
